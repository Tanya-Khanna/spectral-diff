# ===========================================
# Spectral Diff Environment Variables
# ===========================================

# ----- Web (apps/web) -----

# API URL - Required for real mode
# Local: http://localhost:5050
# Production: https://your-api.railway.app
NEXT_PUBLIC_API_URL=http://localhost:5050

# Demo Mode - Set to 'true' to use mock data (no GitHub API calls)
# Useful for demos, testing, and preview deployments
NEXT_PUBLIC_DEMO_MODE=false

# ----- API (services/api) -----

# Server port (platforms like Railway/Fly set this automatically)
PORT=5050

# CORS origin - Required for production
# Set to your Vercel frontend URL
# Example: https://spectral-diff.vercel.app
CORS_ORIGIN=http://localhost:3000

# ----- Local Development -----

# GitHub credentials for local testing (stored in browser localStorage in production)
# These are NOT used by the app - just for reference
# GITHUB_TOKEN=ghp_your_token_here
# GITHUB_OWNER=your-org-or-username
# GITHUB_REPO=your-repo
